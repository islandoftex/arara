!config
# Arara, the cool TeX automation tool
# Copyright (c) 2020, Island of TeX
# All rights reserved.
#
# This rule is part of arara.
identifier: uplatex
name: upLaTeX
authors:
- Island of TeX
commands:
- name: upLaTeX engine
  command: >
    @{
        return getCommand('uplatex' + branch, interaction, shell,
               synctex, options, reference);
    }
arguments:
- identifier: branch
  flag: >
    @{
        if ([ '', 'dev' ].contains(parameters.branch) && parameters.branch != '') {
            return '-' + parameters.branch;
        } else {
            throwError('There are only two predefined branches: empty and dev');
        }
    }
- identifier: interaction
  flag: >
    @{
        if ([ 'batchmode', 'nonstopmode', 'scrollmode',
              'errorstopmode' ].contains(parameters.interaction)) {
            return '--interaction='.concat(parameters.interaction);
        }
        else {
            throwError('The provided interaction value is not valid.');
        }
    }
- identifier: shell
  flag: >
    @{
        isTrue(parameters.shell, '--shell-escape', '--no-shell-escape')
    }
- identifier: synctex
  flag: >
    @{
        isTrue(parameters.synctex, '--synctex=1', '--synctex=0')
    }
- identifier: options
  flag: >
    @{
        if (isList(parameters.options)) {
            return parameters.options;
        }
        else {
            throwError('I was expecting a list of options.');
        }
    }
