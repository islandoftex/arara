!config
identifier: echofour
name: Test rule for conditionals 4
commands:
  - name: The echoer
    command: >
      @{
          return isWindows(getCommand('cmd', '/c', 'echo', 'QuackFour'), getCommand('echo', 'QuackFour'));
      }
arguments: []
