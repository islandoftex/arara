!config
identifier: echothree
name: Test rule for conditionals 3
commands:
  - name: The echoer
    command: >
      @{
          return isWindows(getCommand('cmd', '/c', 'echo', 'QuackThree'), getCommand('echo', 'QuackThree'));
      }
arguments: []
